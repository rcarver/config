#!/usr/bin/env ruby

require 'config'

usage = "usage: config-try-blueprint NODE_FQN"

fqn = ARGV.shift or abort usage

Config.log_to $stdout

project = Config::Project.new(File.expand_path("."))
project.execute_node(fqn)

