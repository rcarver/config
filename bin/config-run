#!/usr/bin/env ruby

require 'config'

usage = "usage: config-run [FQN]"

fqn = ARGV.shift || `hostname`.chomp

Config.log_to $stdout

project = Config::Project.new(File.expand_path("."))
project.update_node!(fqn)
project.execute_node!(fqn)

